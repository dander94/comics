{% extends 'base.html' %}
{% block content %}
{%load staticfiles %}
<link href="{% static 'comic/tablas.css' %}" rel="stylesheet" type="text/css" media="all" />
<h1>Colecciones</h1>

<div id="form_loc"><form action="{% url 'comics:create_col' %}" method="POST" >

 {% csrf_token %}
	<table id="tabla_formulario">
		<tr><td>Añadir nueva colección</td></tr>
		<tr><td>
			{% if messages %}
    				{% for message in messages %}
   					 <p style="text-color:red"><span style="color:red;font-weight:bold">{{ message }}</span></p>
    				{% endfor %}
			{% endif %}			
			{{form.as_p}}
			<input type="submit" value="Crear"></table></td></tr>	

</form>
{% if collection_list %}
	<table id="tabla_formulario">
		<tr><td>Colecciones creadas</td></tr>
	{% for collection in collection_list %}
		<tr>	
		<td>{{collection.nombre}}({{collection.comic_set.all|length}}/{{collection.numeros_totales}})</td>
		</tr>
	{% endfor %}
		
	</table>
{% else %}
	<p>No se han creado colecciones.</p>
{% endif %}
</div>
{%endblock %}


